---
- name: gNMI Capabilities
  gather_facts: false
  hosts: all

  vars:
    ansible_connection: nokia.openconfig.gnmi
    
  tasks:
  - name: gNMI Capabilities
    nokia.openconfig.gnmi_capabilities:
    register: testout

  - name: dump test output
    debug:
      msg: {{ testout.output }}
