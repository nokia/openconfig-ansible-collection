---
- name: gNMI Capabilities
  gather_facts: false
  hosts: all

  tasks:
  - name: gNMI Capabilities
    nokia.openconfig.gnmi_capabilities:
    register: testout

  - name: dump test output
    debug:
      msg: '{{ testout.output }}'
